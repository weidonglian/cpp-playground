CPMAddPackage(
  NAME fmt
  GIT_REPOSITORY https://github.com/fmtlib/fmt.git
  GIT_TAG 11.0.2
  OPTIONS # Add specific options if needed
    "FMT_INSTALL OFF"         # Option to install fmt library
)